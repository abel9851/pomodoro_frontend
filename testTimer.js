const timeDisplay = document.getElementById("timeDisplay");
const button = document.getElementById("startButton");

let timerInterval = null;

// 새로 배우는 것은 3번 반복하기
function timerInterval() {
    if (timerInverval) return; // Prevent multiple intervals
};